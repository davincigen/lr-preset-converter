export default function Tips() {
  return (
    <section className="card tips">
      <h3>Tips</h3>
      <ul>
        <li>Use original exported preset files for best compatibility.</li>
        <li>Converting to `.dng` is supported only when source files are also `.dng`.</li>
        <li>Batch mode works best with files from the same editing profile family.</li>
      </ul>
    </section>
  );
}
